# Legacy file — prefer repo-root .env.deploy (see .env.deploy.example).
# Kept so old docs still work if someone copies this path.

DEPLOY_HOST=89.42.199.39
DEPLOY_USER=root
DEPLOY_PASS='your-root-password'
USE_DOCKER=0
ENSURE_AUTOSTART=1
DEPLOY_DIR=/root/InterpirceORS/publish
SERVICE=agency.service
STABLE_PORT=5005
